predicates:
  clear:
  at:
  at-goal:
  IS-GOAL:
    static: true
  IS-NONGOAL:
    static: true
  MOVE-DIR:
    static: true

actions:
  move:
  push-to-nongoal:
  push-to-goal:

meta:
  max_steps: 100
  step_penalty: -0.01
  invalid_penalty: -0.1
  enable_numeric: true
