predicates:
  road:
    static: true
  at:
  in:
  capacity:
  capacity-predecessor:
    static: true

actions:
  drive:
  pick-up:
  drop:

meta:
  max_steps: 60
  step_penalty: -0.01
  invalid_penalty: -0.1
  enable_numeric: true
