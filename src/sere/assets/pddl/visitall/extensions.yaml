predicates:
  connected:
    nl: "{x} is adjacent to {y}"
    static: true
  at-robot:
    nl: "the robot is at {x}"
  visited:
    nl: "{x} has been visited"

actions:
  move:
    nl: "move the robot from {curpos} to {nextpos}"

meta:
  max_steps: 80
  step_penalty: -0.01
  invalid_penalty: -0.1
